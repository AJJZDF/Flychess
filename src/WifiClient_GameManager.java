
public class WifiClient_GameManager {
    /*
    void sendMSG();

    actionListener 点击筛子
                    生成数值，send_to_server（筛子）

    actionListener 点击哪个棋子
                    生成数值，send_to_server（当前玩家的棋子id）


    写一个内部类  副 recv_thread(){
        while(true)
            receive msg
            //msg类型：
            //1.    收逻辑序列
            //      更新界面
            //2.    收这个是 whoes turn
            //      （只有收到这个消息，才set筛子true，否则其他所有都要setFalse防止误触打乱逻辑）
            //      set界面有筛子，其他的界面控件setFalse
            //3.    收当前玩家可以点击的棋子
            //      （只有收到这个消息，才set棋子true，否则其他所有都要setFalse防止误触打乱逻辑）
            //      set界面上的棋子true
    }

     */
}